#include <asf.h>
#include <stdio.h>
#include <avr/io.h>
#include "uart.h"
#include "util/delay.h"
// POLLING for PART 0
int main (void)
{
	board_init();
	PORTB = 0x20;
	DDRB = 0x20;
	while(1) {
		while (!(PINB & 0x01));
		PORTB = 0x00;
		while ((PINB & 0x01));
		PORTB = 0x20;
}
